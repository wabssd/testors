-- Modern Teleport Özellikli Admin Menu
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local TS = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- Modern UI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ModernTeleportMenu"
screenGui.Parent = game.CoreGui

-- Ana Menü
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 800, 0, 600)
mainFrame.Position = UDim2.new(0.5, -400, 0.5, -300)
mainFrame.BackgroundColor3 = Color3.fromRGB(13, 17, 23)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(48, 54, 61)
mainStroke.Thickness = 2
mainStroke.Parent = mainFrame

-- Başlık
local titleFrame = Instance.new("Frame")
titleFrame.Size = UDim2.new(1, 0, 0, 60)
titleFrame.Position = UDim2.new(0, 0, 0, 0)
titleFrame.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
titleFrame.BorderSizePixel = 0
titleFrame.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 12)
titleCorner.Parent = titleFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(0.5, 0, 1, 0)
titleLabel.Position = UDim2.new(0, 20, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.Text = "VTRL Tweaks"
titleLabel.TextSize = 24
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = titleFrame

local subtitleLabel = Instance.new("TextLabel")
subtitleLabel.Size = UDim2.new(0.5, 0, 0, 20)
subtitleLabel.Position = UDim2.new(0, 20, 0, 35)
subtitleLabel.BackgroundTransparency = 1
subtitleLabel.TextColor3 = Color3.fromRGB(139, 148, 158)
subtitleLabel.Text = "Type to Search for Tweaks"
subtitleLabel.TextSize = 12
subtitleLabel.Font = Enum.Font.Gotham
subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
subtitleLabel.Parent = titleFrame

local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -40, 0, 15)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
closeButton.TextColor3 = Color3.new(1, 1, 1)
closeButton.Text = "X"
closeButton.TextSize = 16
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = titleFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(1, 0)
closeCorner.Parent = closeButton

-- Arama Kutusu
local searchBox = Instance.new("TextBox")
searchBox.Size = UDim2.new(0.4, 0, 0, 40)
searchBox.Position = UDim2.new(0.55, 0, 0, 10)
searchBox.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
searchBox.TextColor3 = Color3.new(1, 1, 1)
searchBox.PlaceholderText = "🔍 Type to Search for Tweaks"
searchBox.PlaceholderColor3 = Color3.fromRGB(139, 148, 158)
searchBox.Text = ""
searchBox.TextSize = 14
searchBox.Font = Enum.Font.Gotham
searchBox.Parent = titleFrame

local searchCorner = Instance.new("UICorner")
searchCorner.CornerRadius = UDim.new(0, 8)
searchCorner.Parent = searchBox

local searchStroke = Instance.new("UIStroke")
searchStroke.Color = Color3.fromRGB(48, 54, 61)
searchStroke.Thickness = 2
searchStroke.Parent = searchBox

-- Sol Sekme Menüsü
local tabFrame = Instance.new("Frame")
tabFrame.Size = UDim2.new(0, 200, 1, -60)
tabFrame.Position = UDim2.new(0, 0, 0, 60)
tabFrame.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
tabFrame.BorderSizePixel = 0
tabFrame.Parent = mainFrame

-- Sağ İçerik Alanı
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -200, 1, -60)
contentFrame.Position = UDim2.new(0, 200, 0, 60)
contentFrame.BackgroundColor3 = Color3.fromRGB(13, 17, 23)
contentFrame.BorderSizePixel = 0
contentFrame.Parent = mainFrame

-- Sekmeler
local tabs = {
    {name = "🏠 Home", id = "home"},
    {name = "🚀 Player", id = "player"},
    {name = "🌀 Teleport", id = "teleport"},
    {name = "🎮 Game", id = "game"},
    {name = "🔧 Tools", id = "tools"}
}

local tabButtons = {}
local currentTab = "home"

-- Değişkenler
local flying = false
local noclip = false
local godmode = false
local bodyVelocity
local bodyGyro
local toggleButtons = {}

-- Fonksiyonlar
function createTabFrame(tabId)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.Position = UDim2.new(0, 0, 0, 0)
    frame.BackgroundTransparency = 1
    frame.Visible = false
    frame.Parent = contentFrame
    frame.Name = tabId
    return frame
end

function createScrollFrame(parent)
    local scroll = Instance.new("ScrollingFrame")
    scroll.Size = UDim2.new(1, 0, 1, 0)
    scroll.Position = UDim2.new(0, 0, 0, 0)
    scroll.BackgroundTransparency = 1
    scroll.ScrollBarThickness = 6
    scroll.ScrollBarImageColor3 = Color3.fromRGB(48, 54, 61)
    scroll.CanvasSize = UDim2.new(0, 0, 0, 800)
    scroll.Parent = parent
    return scroll
end

function createToggleButton(name, desc, recommended, yPos, parent, category)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 80)
    frame.Position = UDim2.new(0, 10, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    frame.Name = name
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(48, 54, 61)
    stroke.Thickness = 1
    stroke.Parent = frame
    
    -- Başlık
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(0.7, 0, 0, 25)
    titleLabel.Position = UDim2.new(0, 15, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.TextColor3 = Color3.new(1, 1, 1)
    titleLabel.Text = name
    titleLabel.TextSize = 16
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = frame
    
    -- Açıklama
    local descLabel = Instance.new("TextLabel")
    descLabel.Size = UDim2.new(0.7, -10, 0, 35)
    descLabel.Position = UDim2.new(0, 15, 0, 35)
    descLabel.BackgroundTransparency = 1
    descLabel.TextColor3 = Color3.fromRGB(139, 148, 158)
    descLabel.Text = desc
    descLabel.TextSize = 12
    descLabel.Font = Enum.Font.Gotham
    descLabel.TextXAlignment = Enum.TextXAlignment.Left
    descLabel.TextYAlignment = Enum.TextYAlignment.Top
    descLabel.Parent = frame
    
    -- Recommended badge
    if recommended then
        local recommendedLabel = Instance.new("TextLabel")
        recommendedLabel.Size = UDim2.new(0, 90, 0, 20)
        recommendedLabel.Position = UDim2.new(0, 15, 0, 55)
        recommendedLabel.BackgroundColor3 = Color3.fromRGB(35, 134, 54)
        recommendedLabel.TextColor3 = Color3.new(1, 1, 1)
        recommendedLabel.Text = "Recommended"
        recommendedLabel.TextSize = 10
        recommendedLabel.Font = Enum.Font.GothamBold
        recommendedLabel.Parent = frame
        
        local recommendedCorner = Instance.new("UICorner")
        recommendedCorner.CornerRadius = UDim.new(0, 4)
        recommendedCorner.Parent = recommendedLabel
    end
    
    -- Toggle Butonu
    local toggleButton = Instance.new("TextButton")
    toggleButton.Size = UDim2.new(0, 100, 0, 35)
    toggleButton.Position = UDim2.new(1, -120, 0, 20)
    toggleButton.BackgroundColor3 = Color3.fromRGB(33, 38, 45)
    toggleButton.TextColor3 = Color3.new(1, 1, 1)
    toggleButton.Text = "OFF"
    toggleButton.TextSize = 12
    toggleButton.Font = Enum.Font.GothamBold
    toggleButton.Parent = frame
    
    local toggleCorner = Instance.new("UICorner")
    toggleCorner.CornerRadius = UDim.new(0, 6)
    toggleCorner.Parent = toggleButton
    
    local toggleStroke = Instance.new("UIStroke")
    toggleStroke.Color = Color3.fromRGB(48, 54, 61)
    toggleStroke.Thickness = 2
    toggleStroke.Parent = toggleButton
    
    -- Toggle'ı kaydet
    toggleButtons[name] = {
        button = toggleButton,
        enabled = false,
        category = category,
        name = name
    }
    
    -- Click event
    toggleButton.MouseButton1Click:Connect(function()
        toggleFeature(name)
    end)
    
    return frame
end

function createTeleportButton(name, desc, yPos, parent)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 100)
    frame.Position = UDim2.new(0, 10, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = frame
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(48, 54, 61)
    stroke.Thickness = 1
    stroke.Parent = frame
    
    -- Başlık
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -20, 0, 25)
    titleLabel.Position = UDim2.new(0, 15, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.TextColor3 = Color3.new(1, 1, 1)
    titleLabel.Text = name
    titleLabel.TextSize = 16
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = frame
    
    -- Açıklama
    local descLabel = Instance.new("TextLabel")
    descLabel.Size = UDim2.new(1, -20, 0, 20)
    descLabel.Position = UDim2.new(0, 15, 0, 35)
    descLabel.BackgroundTransparency = 1
    descLabel.TextColor3 = Color3.fromRGB(139, 148, 158)
    descLabel.Text = desc
    descLabel.TextSize = 12
    descLabel.Font = Enum.Font.Gotham
    descLabel.TextXAlignment = Enum.TextXAlignment.Left
    descLabel.Parent = frame
    
    -- Oyuncu adı input
    local playerInput = Instance.new("TextBox")
    playerInput.Size = UDim2.new(0.6, 0, 0, 30)
    playerInput.Position = UDim2.new(0, 15, 0, 60)
    playerInput.BackgroundColor3 = Color3.fromRGB(33, 38, 45)
    playerInput.TextColor3 = Color3.new(1, 1, 1)
    playerInput.PlaceholderText = "Oyuncu adı yaz..."
    playerInput.PlaceholderColor3 = Color3.fromRGB(139, 148, 158)
    playerInput.Text = ""
    playerInput.TextSize = 12
    playerInput.Font = Enum.Font.Gotham
    playerInput.Parent = frame
    
    local inputCorner = Instance.new("UICorner")
    inputCorner.CornerRadius = UDim.new(0, 6)
    inputCorner.Parent = playerInput
    
    local inputStroke = Instance.new("UIStroke")
    inputStroke.Color = Color3.fromRGB(48, 54, 61)
    inputStroke.Thickness = 2
    inputStroke.Parent = playerInput
    
    -- Teleport Butonu
    local teleportButton = Instance.new("TextButton")
    teleportButton.Size = UDim2.new(0, 120, 0, 30)
    teleportButton.Position = UDim2.new(1, -140, 0, 60)
    teleportButton.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
    teleportButton.TextColor3 = Color3.new(1, 1, 1)
    teleportButton.Text = "🚀 Işınlan"
    teleportButton.TextSize = 12
    teleportButton.Font = Enum.Font.GothamBold
    teleportButton.Parent = frame
    
    local buttonCorner = Instance.new("UICorner")
    buttonCorner.CornerRadius = UDim.new(0, 6)
    buttonCorner.Parent = teleportButton
    
    local buttonStroke = Instance.new("UIStroke")
    buttonStroke.Color = Color3.fromRGB(100, 150, 255)
    buttonStroke.Thickness = 2
    buttonStroke.Parent = teleportButton
    
    -- Teleport fonksiyonu
    teleportButton.MouseButton1Click:Connect(function()
        local targetName = playerInput.Text
        if targetName and targetName ~= "" then
            teleportToPlayer(targetName)
        else
            playerInput.PlaceholderText = "Önce oyuncu adı yaz!"
            wait(1)
            playerInput.PlaceholderText = "Oyuncu adı yaz..."
        end
    end)
    
    return frame
end

function teleportToPlayer(playerName)
    local targetPlayer = findPlayer(playerName)
    if targetPlayer and targetPlayer.Character then
        local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        local myCharacter = player.Character
        local myRoot = myCharacter and myCharacter:FindFirstChild("HumanoidRootPart")
        
        if targetRoot and myRoot then
            myRoot.CFrame = targetRoot.CFrame + Vector3.new(0, 3, 0)
            print("✅ " .. targetPlayer.Name .. " adlı oyuncuya ışınlanıldı!")
        else
            print("❌ Hedef oyuncu bulunamadı!")
        end
    else
        print("❌ Oyuncu bulunamadı: " .. playerName)
    end
end

function findPlayer(name)
    name = name:lower()
    for _, plr in pairs(Players:GetPlayers()) do
        if plr.Name:lower():find(name) or plr.DisplayName:lower():find(name) then
            return plr
        end
    end
    return nil
end

function toggleFeature(name)
    local data = toggleButtons[name]
    if not data then return end
    
    data.enabled = not data.enabled
    
    if data.enabled then
        data.button.BackgroundColor3 = Color3.fromRGB(35, 134, 54) -- Yeşil
        data.button.Text = "ON"
        activateFeature(name)
    else
        data.button.BackgroundColor3 = Color3.fromRGB(33, 38, 45) -- Gri
        data.button.Text = "OFF"
        deactivateFeature(name)
    end
end

function activateFeature(name)
    if name == "Fly Mode" then
        flying = true
        local character = player.Character
        if character then
            local root = character:FindFirstChild("HumanoidRootPart")
            if root then
                bodyVelocity = Instance.new("BodyVelocity")
                bodyVelocity.Velocity = Vector3.new(0, 0, 0)
                bodyVelocity.MaxForce = Vector3.new(40000, 40000, 40000)
                bodyVelocity.Parent = root
                
                bodyGyro = Instance.new("BodyGyro")
                bodyGyro.MaxTorque = Vector3.new(50000, 50000, 50000)
                bodyGyro.P = 10000
                bodyGyro.D = 1000
                bodyGyro.CFrame = root.CFrame
                bodyGyro.Parent = root
            end
        end
    elseif name == "NoClip Mode" then
        noclip = true
    elseif name == "God Mode" then
        godmode = true
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.MaxHealth = math.huge
                humanoid.Health = math.huge
            end
        end
    elseif name == "Speed Boost" then
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = 50
            end
        end
    elseif name == "Player ESP" then
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                local highlight = Instance.new("Highlight")
                highlight.FillColor = Color3.new(1, 0, 0)
                highlight.OutlineColor = Color3.new(1, 1, 0)
                highlight.FillTransparency = 0.5
                highlight.OutlineTransparency = 0
                highlight.Parent = otherPlayer.Character
            end
        end
    elseif name == "Invisibility" then
        local character = player.Character
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 0.8
                end
            end
        end
    end
end

function deactivateFeature(name)
    if name == "Fly Mode" then
        flying = false
        if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
        if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
    elseif name == "NoClip Mode" then
        noclip = false
    elseif name == "God Mode" then
        godmode = false
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.MaxHealth = 100
                humanoid.Health = 100
            end
        end
    elseif name == "Speed Boost" then
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = 16
            end
        end
    elseif name == "Player ESP" then
        for _, otherPlayer in pairs(Players:GetPlayers()) do
            if otherPlayer.Character then
                local highlight = otherPlayer.Character:FindFirstChild("Highlight")
                if highlight then
                    highlight:Destroy()
                end
            end
        end
    elseif name == "Invisibility" then
        local character = player.Character
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 0
                end
            end
        end
    end
end

function switchTab(tabId)
    currentTab = tabId
    
    -- Tüm sekmeleri gizle
    for _, tab in ipairs(tabs) do
        local frame = contentFrame:FindFirstChild(tab.id)
        if frame then
            frame.Visible = false
        end
        if tabButtons[tab.id] then
            tabButtons[tab.id].BackgroundColor3 = Color3.fromRGB(33, 38, 45)
            tabButtons[tab.id].TextColor3 = Color3.fromRGB(139, 148, 158)
        end
    end
    
    -- Aktif sekme
    local activeFrame = contentFrame:FindFirstChild(tabId)
    if activeFrame then
        activeFrame.Visible = true
    end
    if tabButtons[tabId] then
        tabButtons[tabId].BackgroundColor3 = Color3.fromRGB(0, 100, 200)
        tabButtons[tabId].TextColor3 = Color3.new(1, 1, 1)
    end
end

-- Sekmeleri oluştur
for i, tab in ipairs(tabs) do
    local tabButton = Instance.new("TextButton")
    tabButton.Size = UDim2.new(1, -10, 0, 50)
    tabButton.Position = UDim2.new(0, 5, 0, 10 + (i * 55))
    tabButton.BackgroundColor3 = Color3.fromRGB(33, 38, 45)
    tabButton.TextColor3 = Color3.fromRGB(139, 148, 158)
    tabButton.Text = tab.name
    tabButton.TextSize = 14
    tabButton.Font = Enum.Font.Gotham
    tabButton.Parent = tabFrame
    
    local tabCorner = Instance.new("UICorner")
    tabCorner.CornerRadius = UDim.new(0, 8)
    tabCorner.Parent = tabButton
    
    tabButtons[tab.id] = tabButton
    
    tabButton.MouseButton1Click:Connect(function()
        switchTab(tab.id)
    end)
end

-- Home Sekmesi
local homeFrame = createTabFrame("home")
local homeScroll = createScrollFrame(homeFrame)

local welcomeLabel = Instance.new("TextLabel")
welcomeLabel.Size = UDim2.new(1, -20, 0, 100)
welcomeLabel.Position = UDim2.new(0, 10, 0, 10)
welcomeLabel.BackgroundColor3 = Color3.fromRGB(22, 27, 34)
welcomeLabel.TextColor3 = Color3.new(1, 1, 1)
welcomeLabel.Text = "🎮 VTRL Tweaks - Admin Panel\n\nWelcome! Use the menu on the left to access various features and optimizations for your game experience."
welcomeLabel.TextSize = 16
welcomeLabel.Font = Enum.Font.Gotham
welcomeLabel.TextYAlignment = Enum.TextYAlignment.Top
welcomeLabel.Parent = homeScroll

local welcomeCorner = Instance.new("UICorner")
welcomeCorner.CornerRadius = UDim.new(0, 8)
welcomeCorner.Parent = welcomeLabel

-- Player Sekmesi
local playerFrame = createTabFrame("player")
local playerScroll = createScrollFrame(playerFrame)

local playerTweaks = {
    {name = "Fly Mode", desc = "Fly with WASD + Space/Shift controls", recommended = true},
    {name = "NoClip Mode", desc = "Walk through walls and objects", recommended = true},
    {name = "God Mode", desc = "Become immortal and prevent fall damage", recommended = true},
    {name = "Speed Boost", desc = "Increase movement speed for faster travel", recommended = true},
    {name = "Player ESP", desc = "See other players through walls", recommended = false},
    {name = "Invisibility", desc = "Become invisible to other players", recommended = false}
}

for i, tweak in ipairs(playerTweaks) do
    createToggleButton(tweak.name, tweak.desc, tweak.recommended, 10 + (i * 90), playerScroll, "player")
end

-- Teleport Sekmesi
local teleportFrame = createTabFrame("teleport")
local teleportScroll = createScrollFrame(teleportFrame)

-- Oyuncuya Işınlanma
createTeleportButton("🌀 Oyuncuya Işınlan", "Bir oyuncunun adını yaz ve ona ışınlan", 10, teleportScroll)

-- Tüm Oyuncular Listesi
local playersLabel = Instance.new("TextLabel")
playersLabel.Size = UDim2.new(1, -20, 0, 30)
playersLabel.Position = UDim2.new(0, 10, 0, 130)
playersLabel.BackgroundTransparency = 1
playersLabel.TextColor3 = Color3.new(1, 1, 1)
playersLabel.Text = "🔍 Sunucudaki Oyuncular:"
playersLabel.TextSize = 14
playersLabel.Font = Enum.Font.GothamBold
playersLabel.TextXAlignment = Enum.TextXAlignment.Left
playersLabel.Parent = teleportScroll

-- Oyuncu butonlarını oluştur
local function updatePlayerList()
    local playerButtonsFrame = teleportScroll:FindFirstChild("PlayerButtons")
    if playerButtonsFrame then
        playerButtonsFrame:Destroy()
    end
    
    playerButtonsFrame = Instance.new("Frame")
    playerButtonsFrame.Size = UDim2.new(1, -20, 0, 300)
    playerButtonsFrame.Position = UDim2.new(0, 10, 0, 170)
    playerButtonsFrame.BackgroundTransparency = 1
    playerButtonsFrame.Name = "PlayerButtons"
    playerButtonsFrame.Parent = teleportScroll
    
    local yPos = 0
    for _, otherPlayer in pairs(Players:GetPlayers()) do
        if otherPlayer ~= player then
            local playerButton = Instance.new("TextButton")
            playerButton.Size = UDim2.new(1, 0, 0, 40)
            playerButton.Position = UDim2.new(0, 0, 0, yPos)
            playerButton.BackgroundColor3 = Color3.fromRGB(33, 38, 45)
            playerButton.TextColor3 = Color3.new(1, 1, 1)
            playerButton.Text = "👤 " .. otherPlayer.Name
            playerButton.TextSize = 12
            playerButton.Font = Enum.Font.Gotham
            playerButton.Parent = playerButtonsFrame
            
            local buttonCorner = Instance.new("UICorner")
            buttonCorner.CornerRadius = UDim.new(0, 6)
            buttonCorner.Parent = playerButton
            
            playerButton.MouseButton1Click:Connect(function()
                teleportToPlayer(otherPlayer.Name)
            end)
            
            yPos = yPos + 45
        end
    end
end

-- Oyuncu listesini güncelle
updatePlayerList()
Players.PlayerAdded:Connect(updatePlayerList)
Players.PlayerRemoving:Connect(updatePlayerList)

-- Game Sekmesi
local gameFrame = createTabFrame("game")
local gameScroll = createScrollFrame(gameFrame)

local gameTweaks = {
    {name = "Admin Tools", desc = "Get special admin tools and weapons", recommended = false},
    {name = "FPS Boost", desc = "Optimize game for better performance", recommended = true}
}

for i, tweak in ipairs(gameTweaks) do
    createToggleButton(tweak.name, tweak.desc, tweak.recommended, 10 + (i * 90), gameScroll, "game")
end

-- Menü sürükleme
local dragging = false
local dragInput, dragStart, startPos

titleFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

titleFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Arama fonksiyonu
searchBox:GetPropertyChangedSignal("Text"):Connect(function()
    local searchText = searchBox.Text:lower()
    
    for name, data in pairs(toggleButtons) do
        local frame = data.button.Parent
        if frame then
            local shouldShow = searchText == "" or name:lower():find(searchText)
            frame.Visible = shouldShow
        end
    end
end)

-- Kapat butonu
closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- Fly kontrolleri
RunService.Heartbeat:Connect(function()
    if flying and bodyVelocity and bodyGyro then
        local character = player.Character
        if character then
            local root = character:FindFirstChild("HumanoidRootPart")
            local cam = workspace.CurrentCamera
            if root and cam then
                bodyGyro.CFrame = CFrame.new(root.Position, root.Position + cam.CFrame.LookVector)
                
                local direction = Vector3.new(0, 0, 0)
                local speed = 100
                
                if UIS:IsKeyDown(Enum.KeyCode.W) then direction = direction + cam.CFrame.LookVector end
                if UIS:IsKeyDown(Enum.KeyCode.S) then direction = direction - cam.CFrame.LookVector end
                if UIS:IsKeyDown(Enum.KeyCode.A) then direction = direction - cam.CFrame.RightVector end
                if UIS:IsKeyDown(Enum.KeyCode.D) then direction = direction + cam.CFrame.RightVector end
                if UIS:IsKeyDown(Enum.KeyCode.Space) then direction = direction + Vector3.new(0, 1, 0) end
                if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then direction = direction + Vector3.new(0, -1, 0) end
                
                if direction.Magnitude > 0 then
                    direction = direction.Unit * speed
                end
                
                bodyVelocity.Velocity = direction
            end
        end
    end
    
    if noclip then
        local character = player.Character
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
end)

-- İlk sekme
switchTab("teleport")

print("🎮 Modern Teleport Menu yüklendi!")
print("✅ Teleport özelliği eklendi")
print("✅ Oyuncu listesi mevcut")
print("✅ Tüm özellikler aktif")
